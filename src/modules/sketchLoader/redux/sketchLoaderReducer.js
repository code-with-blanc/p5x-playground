const INITIAL_STATE = {
  activeSketch: null,
};

const reducer = (state = INITIAL_STATE, action = { type: 'default' }) => {
  switch(action.type) {
    default:
      return state;
  }
};

export default reducer;
